{
  "name": "setup-fortran",
  "version": "1.3.0",
  "description": "Set up Fortran compilers on GitHub Actions runners",
  "homepage": "https://github.com/minhqdao/setup-fortran#readme",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/minhqdao/setup-fortran.git"
  },
  "bugs": {
    "url": "https://github.com/minhqdao/setup-fortran/issues"
  },
  "license": "Apache-2.0",
  "author": {
    "name": "Minh Dao",
    "email": "hello@minhdao.de",
    "url": "https://minhdao.de"
  },
  "type": "commonjs",
  "main": "index.js",
  "scripts": {
    "build": "ncc build src/index.ts -o dist --source-map",
    "build:integration-test": "ncc build src/run_fortran_tests.ts -o dist-tests --source-map",
    "build:verify-installation": "ncc build src/verify_installation.ts -o dist-verify --source-map",
    "format": "prettier --write src/**/*.ts",
    "format:check": "prettier --check src/**/*.ts",
    "lint": "eslint src/**/*.ts",
    "test": "jest",
    "integration-test": "node dist-tests/index.js",
    "verify-installation": "node dist-verify/index.js",
    "all": "npm run format && npm run lint && npm run test && npm run build"
  },
  "dependencies": {
    "@actions/cache": "^5.2.0",
    "@actions/core": "^2.0.3",
    "@actions/exec": "^2.0.0",
    "@actions/tool-cache": "^3.0.1"
  },
  "devDependencies": {
    "@eslint/js": "^10.0.1",
    "@types/jest": "^30.0.0",
    "@types/node": "^26.1.1",
    "@vercel/ncc": "^0.44.1",
    "eslint": "^10.7.0",
    "globals": "^17.7.0",
    "jest": "^30.4.2",
    "prettier": "^3.9.6",
    "ts-jest": "^29.4.12",
    "typescript": "^6.0.3",
    "typescript-eslint": "^8.65.0"
  }
}
